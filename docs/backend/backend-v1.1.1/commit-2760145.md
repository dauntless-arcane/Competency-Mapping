### High-level summary

This commit is an automated merge of the backend branch into the main branch, resolving any merge conflicts.

### What changed

* The `backend/CHANGELOG.md` file was updated to reflect the changes made in the backend branch, including bug fixes, new features, and breaking changes.
* The `package-lock.json` file was updated to reflect the changes made in the backend branch, including new dependencies and version updates.

### Why it likely changed

The commit was likely triggered by an automated workflow that monitors the backend branch for changes and merges them into the main branch when certain conditions are met. The changes were likely made to fix bugs, add new features, and update dependencies.

### Impact on system

The changes made in this commit are likely to have a significant impact on the system, as they update the backend code and dependencies. This may cause issues with existing functionality, especially if the breaking changes are not properly handled.

### Possible risks

* The automated merge may have introduced new bugs or conflicts that need to be addressed.
* The changes made in this commit may break existing functionality or dependencies.
* The update to the `package-lock.json` file may cause issues with package installations or updates.

### Migration notes (if needed)

* Make sure to test the system thoroughly after merging this commit to ensure that all functionality is working as expected.
* Review the changes made in this commit to understand the impact on the system and make any necessary adjustments.
* Consider updating the `package-lock.json` file manually to ensure that all dependencies are properly installed and updated.

### Test recommendations

* Run a full suite of tests to ensure that all functionality is working as expected.
* Test the system under various scenarios to ensure that it is stable and reliable.
* Consider running a code review to ensure that the changes made in this commit are correct and follow best practices.

### Final release note

"Automated merge of backend branch into main branch, resolving merge conflicts and updating dependencies."

### Full markdown explanation

#### Commit Summary

This commit is an automated merge of the backend branch into the main branch, resolving any merge conflicts.

#### Changes

* **backend/CHANGELOG.md**: Updated to reflect changes made in backend branch
	+ Bug fixes:
		- `/api/users/fetch-tests` to added level, duration, recommended
		- Add --legacy-peer-deps flag to @semantic-release/release-notes-generator installation
		- Fix mongodb_atlas_db_uri_with_credentials in file
		- Fix name not coming in /api/users/fetch-tests/
		- Regenerate package-lock.json with legacy peer deps
		- Add missing mongoose import and update survey response with scores
		- Standardize quotes in frontend release workflow
		- Fix result fetch giving null for all responses
		- Fix wrong status code sent
	+ Features:
		- Add @semantic-release/changelog dependency to package.json and package-lock.json
		- Add @semantic-release/exec dependency to package.json and package-lock.json
		- Added Login.js and fixed api for the user-survey
		- Added the saving api for saving data of users to DB
		- Add categoryConstants support in test creation route
		- Backend release automation file added
		- Add frontend release workflow configuration
		- Add frontend release workflow with semantic-release configuration
		- Implement automatic result generation on submission
		- Integrate framer-motion for smooth question transitions and update validation schema
		- Process pending survey responses and auto-generate results
	+ Breaking changes:
		- **survey**: The payload for `POST /api/users/entry` has changed. The `ans` field is no longer a generic object and must now be an array of objects, each with a `questionId` (string) and `value` (number)
* **package-lock.json**: Updated to reflect changes made in backend branch

#### Impact

The changes made in this commit are likely to have a significant impact on the system, as they update the backend code and dependencies. This may cause issues with existing functionality, especially if the breaking changes are not properly handled.

#### Risks

* The automated merge may have introduced new bugs or conflicts that need to be addressed.
* The changes made in this commit may break existing functionality or dependencies.
* The update to the `package-lock.json` file may cause issues with package installations or updates.

#### Migration notes

* Make sure to test the system thoroughly after merging this commit to ensure that all functionality is working as expected.
* Review the changes made in this commit to understand the impact on the system and make any necessary adjustments.
* Consider updating the `package-lock.json` file manually to ensure that all dependencies are properly installed and updated.

#### Test recommendations

* Run a full suite of tests to ensure that all functionality is working as expected.
* Test the system under various scenarios to ensure that it is stable and reliable.
* Consider running a code review to ensure that the changes made in this commit are correct and follow best practices.

#### Final release note

Automated merge of backend branch into main branch, resolving merge conflicts and updating dependencies.