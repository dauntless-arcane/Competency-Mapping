### Commit Analysis

#### High-level summary

This commit adds a new component called `RiasecResult` to display the results of the RIASEC survey. It also integrates this component into the `ResultsPage` to display the results for the specific survey ID.

#### What changed

* A new component `RiasecResult` was added to display the results of the RIASEC survey.
* The `ResultsPage` was modified to integrate the new component and display the results for the specific survey ID.
* The `OceanTestResult` component was modified to remove the summary section.
* The `ResultTraitCards` component was modified to remove the percentage sign from the score.

#### Why it likely changed

The change likely occurred to add support for the RIASEC survey results, which is a different type of survey from the Ocean Test. The new component was created to handle the specific requirements of the RIASEC survey results.

#### Impact on system

The change will allow users to view their RIASEC survey results, which is a new feature. The system will now be able to handle two different types of survey results: Ocean Test and RIASEC.

#### Possible risks

* The new component may not be thoroughly tested, which could lead to bugs or issues.
* The integration with the `ResultsPage` may not be seamless, which could cause issues with the user experience.

#### Migration notes (if needed)

None.

#### Test recommendations

* Test the new `RiasecResult` component to ensure it displays the results correctly.
* Test the integration with the `ResultsPage` to ensure it displays the results for the specific survey ID.
* Test the `OceanTestResult` component to ensure it still displays the results correctly after the modification.

#### Final release note

Added support for RIASEC survey results.

#### Full markdown explanation

### Commit Analysis

#### High-level summary

This commit adds a new component called `RiasecResult` to display the results of the RIASEC survey. It also integrates this component into the `ResultsPage` to display the results for the specific survey ID.

#### What changed

* A new component `RiasecResult` was added to display the results of the RIASEC survey.
* The `ResultsPage` was modified to integrate the new component and display the results for the specific survey ID.
* The `OceanTestResult` component was modified to remove the summary section.
* The `ResultTraitCards` component was modified to remove the percentage sign from the score.

#### Why it likely changed

The change likely occurred to add support for the RIASEC survey results, which is a different type of survey from the Ocean Test. The new component was created to handle the specific requirements of the RIASEC survey results.

#### Impact on system

The change will allow users to view their RIASEC survey results, which is a new feature. The system will now be able to handle two different types of survey results: Ocean Test and RIASEC.

#### Possible risks

* The new component may not be thoroughly tested, which could lead to bugs or issues.
* The integration with the `ResultsPage` may not be seamless, which could cause issues with the user experience.

#### Migration notes (if needed)

None.

#### Test recommendations

* Test the new `RiasecResult` component to ensure it displays the results correctly.
* Test the integration with the `ResultsPage` to ensure it displays the results for the specific survey ID.
* Test the `OceanTestResult` component to ensure it still displays the results correctly after the modification.

#### Final release note

Added support for RIASEC survey results.

### Code Changes

The code changes are as follows:

* A new component `RiasecResult` was added to display the results of the RIASEC survey.
* The `ResultsPage` was modified to integrate the new component and display the results for the specific survey ID.
* The `OceanTestResult` component was modified to remove the summary section.
* The `ResultTraitCards` component was modified to remove the percentage sign from the score.

### Code Explanation

The new `RiasecResult` component is responsible for displaying the results of the RIASEC survey. It uses the `ResultHeader`, `ResultOverallScore`, `ResultRadarChart`, and `ResultTraitCards` components to display the results.

The `ResultsPage` was modified to integrate the new component and display the results for the specific survey ID. It uses the `RiasecResult` component to display the results.

The `OceanTestResult` component was modified to remove the summary section.

The `ResultTraitCards` component was modified to remove the percentage sign from the score.

### Testing

The new `RiasecResult` component should be thoroughly tested to ensure it displays the results correctly. The integration with the `ResultsPage` should also be tested to ensure it displays the results for the specific survey ID. The `OceanTestResult` component should be tested to ensure it still displays the results correctly after the modification.

### Conclusion

This commit adds a new component called `RiasecResult` to display the results of the RIASEC survey. It also integrates this component into the `ResultsPage` to display the results for the specific survey ID. The change will allow users to view their RIASEC survey results, which is a new feature. The system will now be able to handle two different types of survey results: Ocean Test and RIASEC.